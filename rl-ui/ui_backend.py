import rl_ui

# shape_matrix = rl_ui.user_matrix

shape_matrix = [[1, 1, 1],
                [1, 0, 1],
                [1, 1, 1]]

def create_graph_mat(shape_mat):
    graph_mat = shape_mat


def find_neighbours(shape_mat):
    for row_num, row in shape_mat:
        for col_num, residue in row:
            if residue == 1:


{1:['',2,4]}


def gen_hamiltonian(shape_mat):
